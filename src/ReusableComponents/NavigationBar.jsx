import React from 'react';

const NavigationBar = () => {
    return (
        <nav class="navbar sticky-top bg-light navbar_main">
        <div class="container-fluid">
          <a class="navbar-brand" href="#">Sticky top</a>
        </div>
      </nav>
    );
};

export default NavigationBar;